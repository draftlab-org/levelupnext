---
// White content card echoing the original `.section-block`.
export interface Props {
  class?: string;
}
const { class: className = '' } = Astro.props;
---

<section class={`lu-card px-6 py-8 sm:px-10 sm:py-10 ${className}`}>
	<slot />
</section>
